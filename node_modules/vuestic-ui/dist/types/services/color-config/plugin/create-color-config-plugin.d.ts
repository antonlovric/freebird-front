import { App } from 'vue';
import { GlobalConfig } from '../../global-config/global-config';
export declare const setCSSVariable: (name: string, value: string, root: HTMLElement) => void;
export declare const createColorConfigPlugin: (app: App) => {
    renderCSSVarialbes: (colors?: GlobalConfig['colors']) => string | undefined;
    updateColors: (newValue: GlobalConfig['colors']) => void;
};
