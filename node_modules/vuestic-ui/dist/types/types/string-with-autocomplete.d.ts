/** This will accept any string, but will suggest all strings from `T`. Used for better IDE support in component props. */
export declare type StringWithAutocomplete<T> = T | (string & Record<never, never>);
export declare type UnwrapStringWithAutocomplete<T> = T extends StringWithAutocomplete<infer U> ? U : never;
