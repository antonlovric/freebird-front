import { Ref } from 'vue';
import { DataTableColumnInternal, DataTableItem, DataTableRow, DataTableItemKey } from '../types';
export declare const getItemKey: (source: DataTableItem, itemsTrackBy: string | ((item: DataTableItem) => any)) => DataTableItemKey;
interface useRowsProps {
    items: DataTableItem[];
    itemsTrackBy: string | ((item: DataTableItem) => any);
    [prop: string]: unknown;
}
export default function useRows(columns: Ref<DataTableColumnInternal[]>, props: useRowsProps): {
    rowsComputed: import("vue").ComputedRef<DataTableRow[]>;
};
export {};
