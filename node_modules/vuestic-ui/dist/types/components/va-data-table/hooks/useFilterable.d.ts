import { Ref } from 'vue';
import { DataTableRow, DataTableFilterMethod, DataTableItem } from '../types';
interface useFilterableProps {
    filter: string;
    filterMethod: DataTableFilterMethod | undefined;
    [prop: string]: unknown;
}
export declare type TFilteredArgs = {
    items: DataTableItem[];
    itemsIndexes: number[];
};
export declare type TFilterableEmits = (event: 'filtered', arg: TFilteredArgs) => void;
export default function useFilterable(rawRows: Ref<DataTableRow[]>, props: useFilterableProps, emit: TFilterableEmits): {
    filteredRows: import("vue").ComputedRef<DataTableRow[]>;
};
export {};
