import { DataTableColumnSource, DataTableColumnInternal, DataTableItem } from '../types';
interface useColumnsProps {
    columns: DataTableColumnSource[];
    items: DataTableItem[];
    [prop: string]: unknown;
}
export declare const buildTableColumn: (source: DataTableColumnSource, initialIndex: number) => DataTableColumnInternal;
export default function useColumns(props: useColumnsProps): {
    columnsComputed: import("vue").ComputedRef<DataTableColumnInternal[]>;
};
export {};
