import { DataTableCell, DataTableRow, DataTableRowBind, DataTableCellBind } from '../types';
interface useBindingProps {
    rowBind?: DataTableRowBind;
    cellBind?: DataTableCellBind;
}
export default function useBinding(props: useBindingProps): {
    getRowBind: (row: DataTableRow) => Record<string, string>;
    getCellBind: (cell: DataTableCell, row: DataTableRow) => Record<string, string>;
};
export {};
