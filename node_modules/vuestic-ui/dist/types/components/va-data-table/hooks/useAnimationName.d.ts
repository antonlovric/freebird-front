import { Ref } from 'vue';
import { DataTableRow } from '../types';
interface useSelectableProps {
    currentPage: number | undefined;
    animated: boolean;
}
export default function usePaginatedRows(props: useSelectableProps, rows: Ref<DataTableRow[]>): Ref<string>;
export {};
