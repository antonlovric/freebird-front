import { Ref } from 'vue';
import { DataTableRow } from '../types';
interface useSelectableProps {
    perPage: number | undefined;
    currentPage: number | undefined;
    [prop: string]: unknown;
}
export default function usePaginatedRows(sortedRows: Ref<DataTableRow[]>, props: useSelectableProps): {
    paginatedRows: import("vue").ComputedRef<DataTableRow[]>;
};
export {};
