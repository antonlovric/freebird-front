import { Ref } from 'vue';
export declare const useTextareaRowHeight: (textarea: Ref<HTMLTextAreaElement | undefined>) => {
    calculateRowHeight: () => number;
    calculateHeight: () => number;
};
