const isServer = () => typeof window === 'undefined';
const getWindow = () => typeof window === 'undefined' ? undefined : window;
const fakeGlobal = {};
const getGlobal = () => {
    if (isServer()) {
        if (typeof globalThis === 'undefined') {
            return fakeGlobal;
        }
        return globalThis;
    }
    else {
        return window;
    }
};

export { getGlobal as a, getWindow as g, isServer as i };
//# sourceMappingURL=ssr-utils.js.map
