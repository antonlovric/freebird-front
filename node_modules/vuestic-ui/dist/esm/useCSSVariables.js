import { computed } from 'vue';
import { k as kebabCase_1 } from './useBem.js';

const useCSSVariables = (prefix, cb) => {
    return computed(() => Object.entries(cb()).map(([key, value]) => ({ [`--${prefix}-${kebabCase_1(key)}`]: value })));
};

export { useCSSVariables as u };
//# sourceMappingURL=useCSSVariables.js.map
